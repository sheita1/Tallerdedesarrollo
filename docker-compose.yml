version: '3.8'
name: tallerdedesarrollo

services:
  # 1. SERVICIO DE BASE DE DATOS
  database:
# ... (Sin cambios)

  # 2. SERVICIO DE APLICACIÓN
  aplicacion:
    build:
      context: .
      dockerfile: VistaAdministrador/Dockerfile
    ports:
      - "1556:1556"
    environment:
      DB_HOST: database
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      DB_NAME: ${DB_NAME}
      PORT: 1556
      # 🚨 NUEVO: Definición de la variable UPLOAD_DIR para el backend
      UPLOAD_DIR: /app/uploads
    depends_on:
      - database
    networks:
      - tesis_network
    # 🛑 ELIMINACIÓN CLAVE: Ya no mapeamos el volumen externo
    # volumes:
    #   - ./uploads:/app/uploads
    restart: always

# REDES Y VOLÚMENES
# ... (Sin cambios)